version: '2'
services:
  nginx:
    build: ./nginx
    ports:
      - '80:80'
    links:
      - confluence
  confluence:
    build: ./confluence
    ports:
      - '8090:8090'
      - '8091:8091'
    volumes:
      - ./data/confluence-home:/var/atlassian/application-data/confluence
